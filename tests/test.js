var assert = require('assert');


var assert = require('assert');
describe('Array', function() {
  describe('#user', function() {
    it('should return 1 when the user is inserted into the databse', function() {
      assert.equal([1, 2, 3].indexOf(4), -1);
    });
  });
});

